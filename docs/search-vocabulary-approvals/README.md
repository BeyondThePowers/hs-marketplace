# Search vocabulary approval records

The approval command writes immutable review records here. Each record contains
the complete AI proposal, approving editor, timestamp, and accepted candidate
indices. Commit these records with the corresponding vocabulary change.
